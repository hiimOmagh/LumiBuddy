package de.omagh.feature_ar.di;

import dagger.Module;
/*import dagger.Provides;
import de.omagh.core_data.db.AppDatabase;
import de.omagh.core_data.repository.DiaryRepository;
import de.omagh.core_domain.util.AppExecutors;*/

/**
 * Module for AR feature dependencies. Currently empty but prepared for future providers.
 */
@Module
public class ArModule {
   /* @Provides
    static DiaryRepository provideDiaryRepository(AppDatabase db, AppExecutors executors) {
        return new DiaryRepository(db.diaryDao(), executors);
    }*/
}
